openapi: 3.0.0
info:
  title: Streaming ID API
  version: 1.0.0
  description: API for streaming ID service
servers:
  - url: http://localhost:3000
paths:
  /stream:
    get:
      summary: Get stream
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                  status:
                    type: string
